# 🔍 RiskPilot AI 策略分析报告

**📁 分析文件：** id_rule_20260421.xlsx
**⏰ 分析时间：** 2026-04-23 09:47:17
**🏷️ 分析类型：** model_score_eval

---

### 🌐 业务目标基准

| 项目 | 内容 |
|------|------|
| 市场 | 🇮🇩 印尼 |
| 客群 | 🔵 首贷（新客户首次借款） |
| 产品期限 | 7天 |
| 定价 | 35%砍头息 |
| 通过率下限 | 10% |
| 首逾上限 | 50% |
| 优秀通过率 | 15%+ |
| 优秀首逾 | 42%- |

### 📊 模型评估标准

**首贷合格线：**
- AUC ≥ 0.51（薄征信场景）
- KS ≥ 0.01
- PSI < 0.01

**复贷合格线：**
- AUC ≥ 0.60
- KS ≥ 0.25
- PSI < 0.05

### 🎯 核心业务逻辑
- **高收益覆盖高风险**（APR 800%+）
- **快速迭代、快速放款**
- **逾期容忍上限：50%**（超过则亏损）


### 📝 用户需求
**用户补充说明：**  [渠道筛选: 渠道=['Facebook Installs', 'Instagram Installs', 'Off-Facebook Installs', 'TikTok SAN'], 过滤 0 条]

---

## 📊 数据分析师诊断

### 数据质量分析

#### 缺失值分布
首先，我们需要检查数据集中是否存在缺失值。根据提供的信息，逾期概况显示总样本、坏样本和逾期率均为0，这表明数据集中可能没有缺失值。然而，为了确保这一点，我们应该检查数据集的每一列。

```python
# 假设df是Pandas DataFrame包含我们的数据
missing_values = df.isnull().sum()
missing_values
```

#### 异常值检测
异常值可能影响模型的准确性。我们可以通过以下方法检测异常值：

- 使用箱线图或Z-score方法。
- 检查与业务逻辑不符的值。

```python
# 使用箱线图检测异常值
import seaborn as sns
import matplotlib.pyplot as plt

sns.boxplot(data=df)
plt.show()
```

#### 数据分布偏差
我们需要检查数据分布是否与业务逻辑相符。例如，首贷客户的年龄分布是否合理。

```python
# 检查年龄分布
sns.histplot(df['A001'])
plt.show()
```

#### 样本代表性
我们需要确保样本具有代表性。对于首贷客户，我们可以检查以下指标：

- 年龄分布
- 性别比例
- 地域分布

```python
# 检查年龄分布
sns.histplot(df[df['label'] == 0]['A001'])
plt.show()
```

### 客户风险特征分析

#### 首贷客户
对于首贷客户，我们需要关注以下风险特征：

- 欺诈特征：检查是否有异常的申请行为，如短时间内多次申请。
- 多头借贷：检查是否有多个贷款记录。
- 薄征信表现：检查是否有不完整的信用记录。

```python
# 检查多头借贷
多头借贷指标 = df.groupby('customer_id')['order_id'].nunique()
sns.histplot(多头借贷指标[多头借贷指标 > 1])
plt.show()
```

#### 复贷客户
对于复贷客户，我们需要关注以下风险特征：

- 历史还款表现：检查是否有逾期记录。
- 复借频率：检查客户复借的频率。
- 忠诚度：检查客户使用产品的频率。

```python
# 检查历史还款表现
逾期客户 = df[df['label'] == 1]
sns.histplot(逾期客户['A001'])
plt.show()
```

### 特征预测价值分析

#### 特征区分度
我们需要确定哪些特征对预测逾期最有区分度。我们可以通过以下方法：

- 特征重要性分析。
- 特征选择方法。

```python
# 特征重要性分析
import sklearn.ensemble as ensemble

# 假设X是特征，y是标签
model = ensemble.RandomForestClassifier()
model.fit(X, y)
importances = model.feature_importances_
```

#### 特征稳定性
我们需要检查特征是否稳定。这可以通过时间序列分析或特征随时间的变化来实现。

```python
# 特征稳定性分析
# 假设我们有时间序列数据
df['time'] = pd.to_datetime(df['应还款时间'])
df.sort_values('time', inplace=True)
sns.lineplot(data=df, x='time', y='A001')
plt.show()
```

### 异常检测

#### 欺诈或数据造假
我们需要检查是否存在可疑的欺诈或数据造假迹象。这可以通过以下方法：

- 检查异常的申请行为。
- 检查异常的还款行为。

```python
# 检查异常的申请行为
申请频率 = df.groupby('customer_id')['order_id'].transform('count')
sns.histplot(申请频率[申请频率 > 10])
plt.show()
```

#### 异常样本占比和影响
我们需要确定异常样本的占比和影响。这可以通过以下方法：

- 计算异常样本的比例。
- 分析异常样本对模型的影响。

```python
# 计算异常样本的比例
异常样本比例 = len(异常样本) / len(df)
异常样本比例
```

### 优化建议

#### 数据层面
以下是一些针对数据层面的优化建议：

- 完善缺失值处理策略。
- 识别并处理异常值。
- 确保数据分布符合业务逻辑。
- 确保样本具有代表性。

#### 建模和策略层面
以下是一些针对建模和策略层面的优化建议：

- 使用更复杂的模型来捕捉复杂的关系。
- 使用特征工程来创建新的特征。
- 使用交叉验证来评估模型的泛化能力。
- 使用A/B测试来评估策略的有效性。

以上分析基于提供的信息和数据集，实际分析可能需要更详细的数据和更深入的业务理解。

---

## 🤖 金融建模师评估

### 1️⃣ 模型达标情况

#### 模型合格线评估：
- **AUC合格线**：所有模型均未达到0.51的合格线，表现不佳。
- **KS合格线**：所有模型KS值均未达到0.01的合格线，说明模型区分能力不足。
- **PSI警戒线**：所有模型的PSI值均低于0.01，说明模型稳定性较好。

#### 模型优秀标准评估：
- **AUC优秀**：无模型达到0.60的AUC优秀标准。
- **KS优秀**：无模型达到0.23的KS优秀标准。
- **PSI优秀**：部分模型PSI值低于0.1，表现尚可。

#### 不达标模型差距分析：
- **A001**：AUC和KS均低于合格线，可能是因为特征选择不当或模型复杂度不足。
- **A002**：AUC低于合格线，KS值接近合格线，可能是因为模型对某些特征过度依赖。

### 2️⃣ 模型相关性分析

- **模型相关性**：由于只有两个模型，无法准确评估相关性。
- **组合提升效果**：由于模型性能不足，建议先优化单个模型后再考虑组合。
- **互补性**：由于数据有限，无法评估互补性。

### 3️⃣ 稳定性评估（PSI）

- **PSI值**：所有模型PSI值均低于0.01，说明模型稳定性较好。
- **稳定性趋势**：由于数据有限，无法评估稳定性随时间的变化趋势。
- **特征漂移风险**：由于PSI值较低，特征漂移风险较小。

### 4️⃣ 区分度分析

- **分数区间**：由于数据有限，无法进行详细分析。
- **高分段/低分段**：由于数据有限，无法评估区分效果。
- **业务阈值附近**：由于数据有限，无法评估区分能力。

### 5️⃣ 优化建议

#### 模型效果提升：
- **特征工程**：增加更多特征，如用户行为数据、社交媒体数据等。
- **模型复杂度**：尝试更复杂的模型，如集成学习模型。

#### 业务场景调优：
- **特征选择**：优化特征选择，去除冗余特征。
- **模型参数**：调整模型参数，提高模型性能。

#### 特征工程或模型结构改进建议：
- **特征工程**：创建更多衍生特征，如用户活跃度、设备信息等。
- **模型结构**：尝试不同的模型结构，如随机森林、梯度提升树等。

总结：
- 模型性能未达到预期，需要进一步优化。
- 建议从特征工程和模型结构两方面入手，提高模型性能。
- 注意区分首贷和复贷的评估标准，针对不同场景进行优化。

---

## 🎯 风控策略专家建议

1️⃣ **数据诊断**：
   - **问题**：模型性能未达标，AUC和KS均低于合格线，PSI值低于0.01，说明模型区分度和稳定性不足。
   - **亮点**：PSI值较低，说明模型稳定性较好，但需关注潜在的特征漂移风险。
   - **反常**：部分模型对某些特征过度依赖，可能存在模型复杂度过高或特征选择不当的问题。

2️⃣ **洞察提炼**：
   - 模型区分度不足，特征选择和模型复杂度可能存在问题。
   - 模型稳定性较好，但需关注特征漂移风险。
   - 特征工程不足，可能未充分利用用户行为数据、社交媒体数据等。

3️⃣ **策略建议**（按重要性排序）：

   1. **特征工程优化**：
      - **具体操作**：增加用户行为数据、社交媒体数据等新特征，如用户活跃度、设备信息、社交网络关系等。
      - **预期效果**：提高模型区分度，增强模型对欺诈行为的识别能力。
      - **风险提示**：确保新特征与目标变量相关，避免引入噪声。

   2. **模型结构调整**：
      - **具体操作**：尝试使用集成学习模型（如随机森林、梯度提升树）代替单一模型，提高模型复杂度和泛化能力。
      - **预期效果**：提高模型区分度和稳定性，降低特征漂移风险。
      - **风险提示**：注意模型过拟合问题，合理调整模型参数。

   3. **欺诈检测策略**：
      - **具体操作**：加强欺诈检测算法，如使用机器学习算法识别异常申请行为、多头借贷等。
      - **预期效果**：降低欺诈风险，提高首贷逾期率。
      - **风险提示**：确保欺诈检测算法的准确性和效率。

   4. **风险定价策略**：
      - **具体操作**：根据客户风险等级，调整利率和砍头息比例，实现高收益覆盖高风险。
      - **预期效果**：提高整体收益，降低风险。
      - **风险提示**：确保风险定价策略的公平性和透明度。

   5. **首贷和复贷差异化策略**：
      - **具体操作**：针对首贷和复贷客户，制定差异化风控策略，如提高首贷欺诈检测力度，降低复贷利率。
      - **预期效果**：提高整体收益，降低风险。
      - **风险提示**：确保差异化策略的合理性和公平性。

   6. **数据质量监控**：
      - **具体操作**：建立数据质量监控体系，定期检查数据缺失、异常值等问题。
      - **预期效果**：确保数据质量，提高模型性能。
      - **风险提示**：关注数据质量变化，及时调整策略。

---

## 📋 附录：原始分析数据

### 数据摘要
```
【数据规模】1,148行 × 96列
【列名】order_id, customer_id, product, 渠道, 渠道组, 应还款时间, label, A001, A002, A003, A004, A005, A006, A011, A013, A014, APP001, APP002, APP003, APPLY_HOUR

【逾期概况】
  总样本: 0条
  坏样本: 0条
  逾期率: 0.00%
  KS值: 0.0000
  AUC: 0.0000

【Top10重要特征】
  A001: 相关系数=-0.0476
  BD004: 相关系数=nan
  BD005: 相关系数=nan
  BD006: 相关系数=nan
  BJ_behavior: 相关系数=-0.0353
  CTS002: 相关系数=nan
  CTS006: 相关系数=nan
  CTS007: 相关系数=nan
  CTS012: 相关系数=nan
  CTS013: 相关系数=nan
```

### 模型摘要
```
【多模型分析】共0个模型

Top3模型性能：

【分箱分析】
  渠道组: IV=0.0000, 单调性=❌
  A001: IV=0.0212, 单调性=❌
  A002: IV=0.0018, 单调性=✅
```

---
*本报告由 RiskPilot 多专家AI系统自动生成*