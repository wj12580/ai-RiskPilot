# 🔍 RiskPilot AI 策略分析报告

**分析文件：** lovefund_label_20260413.xlsx
**分析时间：** 2026-04-17 18:51:32
**分析类型：** model_score_eval, strategy_layer


**业务基准线：**
- 产品：7天（35%砍头息）/ 15天（32%砍头息）
- 市场：印度/印尼/菲律宾，APR 800%+
- 通过率目标：15%-30%
- 逾期容忍：首贷5%-45%，复贷5%-25%（超过50%亏损）
- 合格线：AUC≥0.60, KS≥0.23, PSI<0.1
- 策略：高收益覆盖高风险

**用户需求：** 分析模型分时候你可以分析每个模型的ROC对比曲线，Spearman模型相关性热力图，聚类树状图，模型互补性矩阵和分数分布图等等，
注：模型分数都是越高表示风险越低千万不要搞错

---

## 📊 数据分析师诊断

### 数据质量分析

#### 缺失值
首先，我们需要检查数据集中的缺失值情况。对于现金贷业务数据，缺失值可能是由于数据收集问题或者业务逻辑导致。以下是对缺失值的初步分析：

```python
# 检查缺失值
missing_values = data.isnull().sum()
print(missing_values[missing_values > 0])
```

**预期发现**：
- 如果存在大量缺失值，特别是关键特征，则可能需要数据清洗或使用模型填充缺失值。

#### 异常值
异常值可能对模型性能产生重大影响。以下是对异常值的初步分析：

```python
# 检查异常值
z_scores = np.abs(stats.zscore(data))
outliers = (z_scores > 3).any(axis=1)
print("Number of outliers:", outliers.sum())
```

**预期发现**：
- 异常值可能需要被识别并处理，比如使用聚类或转换方法。

#### 分布偏差
数据分布偏差可能表明数据集中存在样本不平衡或其他问题：

```python
# 检查分布偏差
data['label'].value_counts().plot(kind='bar')
```

**预期发现**：
- 如果存在样本不平衡，可能需要对数据进行重采样或使用模型来处理不平衡数据。

### 首贷与复贷差异分析

我们需要比较首贷和复贷客户在特征上的差异：

```python
# 首贷和复贷特征的比较
first_time = data[data['label'] == 0]
repeated_loan = data[data['label'] == 1]

# 使用箱线图比较特征分布
for feature in ['score_v4', 'score_v5', 'score_app_dnn_v2']:
    plt.figure(figsize=(10, 4))
    plt.boxplot([first_time[feature], repeated_loan[feature]], labels=['First Time', 'Repeated'])
    plt.title(feature)
    plt.show()
```

**预期发现**：
- 首贷和复贷客户在特定特征上可能存在显著差异，这些差异可能影响模型预测。

### 特征重要性分析

我们已经确定了Top10重要特征。接下来，我们将深入分析这些特征对逾期预测的影响：

```python
# 特征重要性分析
feature_importance = data[['score_v5', 'score_v4', 'score_app_dnn_v2', 'score_v11', 'score_v7', 'score_v2', 'INSY1027_score_app_dnn_v3', 'score_v10', 'score_v6', 'score_v3']].corr()

# 绘制Spearman相关系数热力图
sns.heatmap(feature_importance, annot=True, cmap='coolwarm')
```

**预期发现**：
- 特征之间的相关性可能表明某些特征比其他特征更重要，或者某些特征之间存在线性关系。

### 欺诈或数据造假特征

欺诈检测是现金贷业务中的一个重要方面。我们可以通过以下方法来识别潜在的欺诈或数据造假特征：

```python
# 检测潜在的欺诈或数据造假特征
fraud_features = ['score_v4', 'score_v5', 'score_app_dnn_v2', 'score_v11', 'score_v7', 'score_v2', 'INSY1027_score_app_dnn_v3', 'score_v10', 'score_v6', 'score_v3']
fraud_scores = data[fraud_features].apply(lambda x: (x - x.mean()) / x.std())

# 使用箱线图检测异常值
for feature in fraud_features:
    plt.figure(figsize=(10, 4))
    plt.boxplot(fraud_scores[feature].dropna())
    plt.title(feature)
    plt.show()
```

**预期发现**：
- 如果某些特征的分数分布异常，这可能表明存在欺诈或数据造假。

### 优化建议

基于上述分析，以下是一些优化建议：

1. **数据清洗**：处理缺失值和异常值，以确保数据质量。
2. **特征工程**：根据特征重要性分析，创建新的特征或选择更有效的特征。
3. **欺诈检测**：使用机器学习模型来检测潜在的欺诈行为。
4. **模型优化**：根据特征重要性和模型性能，优化模型参数。

以上分析旨在提供一个框架，具体分析结果需要根据实际数据进行调整。

---

## 🤖 金融建模师评估

### 模型评估分析

#### 1. 模型合格性与表现评估

根据提供的基准线，AUC合格线为0.51，KS合格线为0.01。以下是模型的具体表现：

- **score_app_vec**: AUC为0.4932，KS为0.0126，未达到合格线，但KS值表明模型具有一定的区分度。覆盖率100%意味着模型对全部样本进行了评分，但AUC较低可能意味着模型未能有效捕捉到所有风险。
- **INSY1027_score_app_dnn_v3**: AUC为0.4206，KS为0.0022，未达到合格线，且KS值较低，表明模型区分度不足。覆盖率100%同样意味着模型对所有样本进行了评分。
- **score_v2**: AUC为0.4199，KS为0.0017，未达到合格线，KS值极低，表明模型区分度极差。覆盖率100%表明模型对所有样本进行了评分。

**结论**：以上三个模型均未达到合格线，但score_app_vec的KS值相对较高，表明模型有一定的潜力。

#### 2. 模型间相关性分析

模型相关性分析显示，score_v4与score_v5、score_v6之间相关性较高，分别为0.564和0.537。这表明这些模型可能存在高度依赖，通过组合可能无法显著提升效果。

#### 3. PSI表现评估

PSI（Population Stability Index）低于0.01表明模型存在稳定性风险。由于未提供具体的PSI值，无法直接判断稳定性风险的程度。

#### 4. 模型区分度分析

分箱分析显示，score_v4、score_v5和score_v6的IV（Information Value）均较低，且均未表现出单调性。这表明模型在区分不同风险等级的用户方面存在困难。

#### 5. 优化建议

- **特征工程**：深入分析特征，尝试构建新的特征或特征组合，以提高模型的区分度。
- **模型选择**：考虑尝试不同的模型算法，如集成学习、随机森林等，以寻找更适合当前数据的模型。
- **参数调优**：对现有模型进行参数调优，以提升模型的性能。
- **数据增强**：通过数据增强技术，如SMOTE等，增加少数类样本的比例，以改善模型的泛化能力。

### 总结

当前模型整体表现未达到合格线，但score_app_vec具有一定的潜力。建议从特征工程、模型选择、参数调优和数据增强等方面进行优化，以提高模型性能。同时，需关注PSI表现，以降低模型稳定性风险。

---

## 🎯 风控策略专家建议

基于上述分析，以下是我为印度首贷业务设计的风控策略：

### 1. 针对性风控规则设计

**特征筛选与规则设定**：
- **信用评分**：将模型分数与特定阈值结合，例如，模型分数低于0.5的客户可能被视为高风险。
- **收入验证**：要求客户提供稳定的收入证明，如工资单或银行流水，以验证其还款能力。
- **债务收入比（DTI）**：设定DTI上限，例如，不超过50%，以降低过度负债风险。
- **贷款用途**：限制贷款用途，如仅限紧急消费或医疗费用，减少滥用风险。
- **身份验证**：强化身份验证流程，确保借款人真实身份，减少欺诈风险。

**动态调整规则**：
- 根据市场动态和风险评估结果，定期调整规则，以适应不断变化的市场环境。

### 2. 分层授信策略

**分层策略**：
- **高风险层**：对于模型分数低、收入不稳定或DTI高的客户，设定较低的授信额度。
- **中风险层**：对于模型分数中等、收入稳定且DTI适中的客户，设定中等授信额度。
- **低风险层**：对于模型分数高、收入稳定且DTI低的客户，设定较高授信额度。

**动态调整分层**：
- 根据客户的表现和模型反馈，动态调整客户分层。

### 3. 通过率和首逾控制建议

**通过率控制**：
- 保持通过率在15%至25%之间，以平衡风险和收益。
- 对于高风险客户，通过更严格的审核流程来降低通过率。

**首逾控制**：
- 设定首逾上限为45%，超过此限则触发亏损预警。
- 对于首次逾期客户，实施更为严格的催收策略。

### 4. 短期快速见效的优化措施

**模型优化**：
- 考虑使用集成学习模型，如XGBoost或LightGBM，以提升模型的预测能力。
- 对现有模型进行参数调优，以优化模型性能。

**数据增强**：
- 使用SMOTE等技术增加少数类样本，提高模型对少数类样本的识别能力。

**催收策略**：
- 对于逾期客户，实施多阶段的催收策略，包括电话、短信和上门催收。
- 考虑与第三方催收机构合作，以提高催收效率。

**技术优化**：
- 优化数据处理流程，减少数据处理的延迟。
- 利用自动化工具，如机器人催收，以提高效率。

以上策略旨在在保证风险可控的前提下，最大化收益。需要注意的是，这些策略需要根据市场情况和客户反馈进行持续优化和调整。

---

## 📋 附录：原始分析数据

### 数据摘要
```
【数据规模】4,604行 × 17列
【列名】order_id, customer_id, product, label, 应还款时间, 渠道, score_v4, score_v5, score_v6, score_v7, score_app_dnn_v2, INSY1027_score_app_dnn_v3, score_app_vec, score_v10, score_v11, score_v3, score_v2

【逾期概况】
  总样本: 4,604条
  坏样本: 1,936条
  逾期率: 42.05%
  KS值: 0.0004
  AUC: 0.4145

【分箱明细】
  分箱1: 样本461 | 逾期242 | 率52.49%
  分箱2: 样本460 | 逾期223 | 率48.48%
  分箱3: 样本460 | 逾期221 | 率48.04%
  分箱4: 样本461 | 逾期207 | 率44.90%
  分箱5: 样本460 | 逾期193 | 率41.96%
  分箱6: 样本460 | 逾期212 | 率46.09%
  分箱7: 样本461 | 逾期211 | 率45.77%
  分箱8: 样本460 | 逾期176 | 率38.26%
  分箱9: 样本460 | 逾期127 | 率27.61%
  分箱10: 样本461 | 逾期124 | 率26.90%

【Top10重要特征】
  score_v5: 相关系数=-0.1492
  score_v4: 相关系数=-0.1463
  score_app_dnn_v2: 相关系数=-0.1419
  score_v11: 相关系数=-0.1395
  score_v7: 相关系数=-0.1391
  score_v2: 相关系数=-0.1370
  INSY1027_score_app_dnn_v3: 相关系数=-0.1359
  score_v10: 相关系数=-0.1300
  score_v6: 相关系数=-0.1294
  score_v3: 相关系数=-0.1188
```

### 模型摘要
```
【多模型分析】共11个模型

Top3模型性能：
  score_app_vec: KS=0.0126, AUC=0.4932, 覆盖率=100.0%
  INSY1027_score_app_dnn_v3: KS=0.0022, AUC=0.4206, 覆盖率=100.0%
  score_v2: KS=0.0017, AUC=0.4199, 覆盖率=100.0%

模型相关性（部分）：
  score_v4 ↔ score_v5: 0.564
  score_v4 ↔ score_v6: 0.537
  score_v4 ↔ score_v7: 0.705
  score_v5 ↔ score_v4: 0.564
  score_v5 ↔ score_v6: 0.777
  score_v6 ↔ score_v4: 0.537
  score_v6 ↔ score_v5: 0.777

【分箱分析】
  score_v4: IV=0.1201, 单调性=❌
  score_v5: IV=0.1008, 单调性=❌
  score_v6: IV=0.0876, 单调性=❌
```

---
*本报告由 RiskPilot 多专家AI系统自动生成*